[project]
name = "d3500-capture"
version = "0.1.0"
description = "Local Nikon D3500 USB controller for live preview and still capture"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
